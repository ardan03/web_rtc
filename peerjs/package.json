{
  "name": "y",
  "version": "y",
  "main": "dist/index.js",
  "author": "Anna",
  "license": "MIT",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "yarn build",
    "start": "nodemon .",
    "dev": "concurrently --kill-others \"yarn watch\" \"yarn start\""
  },
  "dependencies": {
    "peer": "^1.0.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.10",
    "tslint": "^6.1.3",
    "typescript": "^4.5.5"
  },
  "description": "y"
}
